Git Submodules
==============

On a fresh clone, make sure to update the submodules. Simply:

    git submodule init
    git submodule update

gist plugin for vim
===================

Add a section like this to your .gitconfig:

    [github]
        user = <username>
        token = <github api token>
